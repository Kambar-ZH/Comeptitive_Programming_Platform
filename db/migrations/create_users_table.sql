CREATE TABLE users (
    handle VARCHAR(255),
    email VARCHAR(255),
    country VARCHAR(255),
    city VARCHAR(255),
    rating INTEGER,
    max_rating INTEGER,
    avatar VARCHAR(255),
    encrypted_password VARCHAR(255)
);